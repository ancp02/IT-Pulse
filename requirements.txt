# requirements.txt — IT Pulse dependencies
#
# All packages are free and open-source.
# No API keys or paid services are required.
#
# Install:  pip install -r requirements.txt

requests==2.33.0      # HTTP client for downloading RSS feeds
feedparser==6.0.11    # RSS / Atom feed parser
Jinja2==3.1.4         # HTML templating engine
